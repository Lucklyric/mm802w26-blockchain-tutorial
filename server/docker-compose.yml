services:
  blockchain:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:8000"
    volumes:
      - chain-data:/app/data
    restart: unless-stopped

volumes:
  chain-data:
